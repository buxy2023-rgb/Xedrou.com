import React, { useEffect, useMemo, useState } from "react";
import { UserPlus, UserMinus, Users, Search, ShieldCheck } from "lucide-react";

const seed = [
  { id: "seed-1", name: "Amina Yusuf", email: "amina@example.com", role: "Accountant", status: "active" },
  { id: "seed-2", name: "David Okafor", email: "david@example.com", role: "Developer", status: "active" },
  { id: "seed-3", name: "Grace Mensah", email: "grace@example.com", role: "Customer Service", status: "active" },
];

export default function HRWorkspace(){
  const company=new URLSearchParams(window.location.search).get("company")||"selected company";
  const key=`xedruo_staff_${company}`;
  const [staff,setStaff]=useState([]); const [name,setName]=useState(""); const [email,setEmail]=useState(""); const [role,setRole]=useState("Staff"); const [search,setSearch]=useState("");
  useEffect(()=>{try{const saved=JSON.parse(localStorage.getItem(key)||"null");setStaff(saved||seed)}catch{setStaff(seed)}},[key]);
  useEffect(()=>{if(staff.length)localStorage.setItem(key,JSON.stringify(staff))},[staff,key]);
  const visible=useMemo(()=>staff.filter(s=>(s.name+s.email+s.role).toLowerCase().includes(search.toLowerCase())),[staff,search]);
  function add(){if(!name.trim()||!email.trim())return;setStaff(s=>[{id:crypto.randomUUID(),name:name.trim(),email:email.trim(),role,status:"active"},...s]);setName("");setEmail("");}
  function remove(id){setStaff(s=>s.map(x=>x.id===id?{...x,status:"removed"}:x));}
  return <div className="min-h-screen bg-slate-950 text-white p-5 md:p-8"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap justify-between gap-4 mb-8"><div><div className="text-xs uppercase tracking-[.3em] text-cyan-300">HR workstation</div><h1 className="mt-2 text-3xl font-bold">Company Staff</h1><p className="mt-2 text-slate-400">{company} · register, assign and remove workforce members</p></div><div className="rounded-2xl bg-white/5 border border-white/10 px-5 py-4 flex items-center gap-3"><Users className="text-cyan-300"/><div><div className="text-2xl font-bold">{staff.filter(s=>s.status==='active').length}</div><div className="text-xs text-slate-500">active staff</div></div></div></div><div className="grid gap-5 lg:grid-cols-[380px_1fr]"><section className="rounded-2xl border border-white/10 bg-white/5 p-6"><div className="flex items-center gap-2 font-semibold"><UserPlus size={18} className="text-cyan-300"/> Register staff</div><div className="mt-5 space-y-3"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Full name" className="w-full rounded-xl bg-black/20 border border-white/10 p-3 outline-none"/><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" type="email" className="w-full rounded-xl bg-black/20 border border-white/10 p-3 outline-none"/><select value={role} onChange={e=>setRole(e.target.value)} className="w-full rounded-xl bg-black/20 border border-white/10 p-3 outline-none"><option>Staff</option><option>Developer</option><option>Accountant</option><option>Customer Service</option><option>HR</option><option>Governor</option></select><button onClick={add} className="w-full rounded-xl bg-cyan-300 text-slate-950 p-3 font-semibold">Register staff member</button></div><div className="mt-6 rounded-xl bg-cyan-300/5 p-4 text-xs text-slate-400 flex gap-2"><ShieldCheck className="text-cyan-300 shrink-0" size={16}/>Staff changes are scoped to the selected company.</div></section><section className="rounded-2xl border border-white/10 bg-white/5 overflow-hidden"><div className="p-5 border-b border-white/10"><div className="relative"><Search className="absolute left-3 top-3 text-slate-500" size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search staff" className="w-full rounded-xl bg-black/20 border border-white/10 p-3 pl-9 outline-none"/></div></div><div className="divide-y divide-white/5">{visible.map(s=><div key={s.id} className="p-5 flex flex-wrap items-center justify-between gap-3"><div><div className="font-semibold">{s.name}</div><div className="text-xs text-slate-500">{s.email} · {s.role}</div></div><div className="flex items-center gap-3"><span className={`rounded-full px-3 py-1 text-xs ${s.status==='active'?"bg-emerald-300/10 text-emerald-300":"bg-red-300/10 text-red-300"}`}>{s.status}</span>{s.status==='active'&&<button onClick={()=>remove(s.id)} className="rounded-xl border border-red-300/20 px-3 py-2 text-xs text-red-300 flex items-center gap-1"><UserMinus size={14}/> Remove</button>}</div></div>)}{visible.length===0&&<div className="p-10 text-center text-slate-500">No staff found.</div>}</div></section></div></div></div>;
}
