import React, { useMemo, useState } from 'react';
import { buildCompanySpecification, buildInitialSitePages, buildInitialRoles } from '../lib/companyBuilder';

const companies = ['Xedruo','Sportruo','Hireruo','Adom','Agruo','Heathrou','Xedruo Education','Xedruo Capital','Xedruo Energy','Xedruo Logistics','Xedruo Properties','Spacetruo','Xedruo AI','Xedruo Power Holdings'];

export default function DeveloperControlCenter() {
  const [selected, setSelected] = useState(companies[0]);
  const [name, setName] = useState(''); const [industry, setIndustry] = useState(''); const [prompt, setPrompt] = useState('');
  const [status, setStatus] = useState('Ready'); const [spec, setSpec] = useState(null);
  const domainPreview = useMemo(() => `${(name || selected).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')}.com`, [name, selected]);
  const generate = () => { try { const next = buildCompanySpecification({ name, industry, description: prompt, domain: domainPreview, features: prompt.split(/\n|,/).map(s=>s.trim()).filter(Boolean) }); setSpec(next); setStatus(`Draft ready: ${next.company.name}`); } catch (e) { setStatus(e.message); } };
  return <main className="min-h-screen bg-slate-950 text-white p-6 md:p-10"><div className="max-w-7xl mx-auto"><div className="mb-8"><p className="text-sm uppercase tracking-[0.25em] text-slate-400">Xedruo Power Holdings</p><h1 className="text-3xl md:text-5xl font-semibold mt-2">Developer Control Center</h1><p className="text-slate-400 mt-3 max-w-3xl">Internal technical command center. Developers can create company specifications for websites, apps, portals and infrastructure without an external website builder.</p></div><div className="grid lg:grid-cols-[280px_1fr] gap-6"><aside className="rounded-2xl border border-slate-800 bg-slate-900 p-4"><h2 className="font-medium mb-3">Companies</h2><div className="space-y-1">{companies.map(c=><button key={c} onClick={()=>setSelected(c)} className={`w-full text-left px-3 py-2 rounded-lg text-sm ${selected===c?'bg-white text-slate-950':'text-slate-300 hover:bg-slate-800'}`}>{c}</button>)}</div></aside><section className="space-y-6"><div className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><div className="flex items-center justify-between gap-4 mb-6"><div><p className="text-sm text-slate-400">Selected company</p><h2 className="text-2xl font-semibold">{selected}</h2></div><span className="px-3 py-1 rounded-full bg-emerald-500/10 text-emerald-300 text-xs">Developer access</span></div><div className="grid md:grid-cols-2 gap-4"><label className="block"><span className="text-sm text-slate-400">New company name</span><input value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-xl bg-slate-950 border border-slate-700 px-4 py-3 outline-none" placeholder="Company name" /></label><label className="block"><span className="text-sm text-slate-400">Industry</span><input value={industry} onChange={e=>setIndustry(e.target.value)} className="mt-2 w-full rounded-xl bg-slate-950 border border-slate-700 px-4 py-3 outline-none" placeholder="Technology, logistics, media..." /></label></div><label className="block mt-4"><span className="text-sm text-slate-400">Build specification</span><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} rows={6} className="mt-2 w-full rounded-xl bg-slate-950 border border-slate-700 px-4 py-3 outline-none" placeholder="Describe the website, app, customer portal, staff tools and business functions." /></label><div className="mt-4 flex flex-wrap items-center gap-3"><button onClick={generate} className="rounded-xl bg-white text-slate-950 px-5 py-3 font-medium">Create internal company specification</button><span className="text-sm text-slate-400">Domain: <strong className="text-slate-200">{domainPreview}</strong></span></div><p className="mt-4 text-sm text-slate-400">Status: {status}</p></div>{spec&&<div className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><h2 className="text-xl font-semibold">Generated company plan</h2><p className="text-slate-400 mt-2">{spec.company.name} · {spec.company.industry} · {spec.company.domain}</p><div className="grid md:grid-cols-2 gap-3 mt-5">{Object.entries(spec.modules).map(([k,v])=><div key={k} className="rounded-xl bg-slate-950 border border-slate-800 p-3 text-sm"><span className="text-slate-300">{k}</span><span className="float-right text-emerald-300">{v?'enabled':'off'}</span></div>)}</div><p className="text-xs text-slate-500 mt-5">Initial pages: {buildInitialSitePages(spec).map(p=>p.slug).join(', ')} · Roles: {buildInitialRoles().join(', ')}</p></div>}<div className="grid md:grid-cols-3 gap-4">{['Website Builder','Application Builder','Infrastructure'].map(item=><div key={item} className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><p className="text-slate-400 text-sm">Developer module</p><h3 className="font-semibold text-lg mt-1">{item}</h3><p className="text-sm text-slate-500 mt-2">Internal Xedruo platform module.</p></div>)}</div></section></div></div></main>;
}
